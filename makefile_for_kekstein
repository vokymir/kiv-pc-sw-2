CC = gcc
CFLAGS = -Wall -pedantic -std=c99
SRC_FILES = $(shell find . -name '*.c')
TARGET = kmas.exe

all: clean $(TARGET)

$(TARGET): $(SRC_FILES)
	$(CC) $(CFLAGS) $(SRC_FILES) -o $@

clean:
	rm -f $(TARGET)
